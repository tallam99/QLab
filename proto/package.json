{
  "name": "qlab-proto-codegen",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling-only package: pins the TypeScript protobuf codegen plugin used by `buf generate` (see buf.gen.yaml). Not the frontend app — that lands in Phase 9.",
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "^2.2.5"
  }
}
